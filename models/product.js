class Procuct {
    constructor(id, ownerId, title, imageUrl, descr, price) {
        this.id = id;
        this.ownerId = ownerId;
        this.title = title;
        this.imageUrl = imageUrl;
        this.descr = descr;
        this.price = price;
    }
}